name: Pipeline Tests
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run actions/checkout
        uses: actions/checkout@v4
      - name: Run actions/checkout
        run: echo "Teste de Steps"

      - name: Cypress run-1
        uses: cypress-io/github-action@v6
        with:
          command: npm run cy:login
